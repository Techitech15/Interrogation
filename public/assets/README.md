# assets (placeholder)

このディレクトリの画像は **仮アセット** です。P1プロトタイプでは容疑者シルエットもCSSグラデーションのみで代用しており、実画像は未使用です。

本番アセット(背景・容疑者シルエット差分・証拠カード画像等)は別途 Codex 側で生成し、このディレクトリ配下のファイルをそのまま差し替える想定です。ファイル名・参照パス(`src/data/cases/*/*.json` の `imageAsset` / `scenePhotos` フィールド)は変更しないこと。
